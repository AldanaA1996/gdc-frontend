---
import Layout  from "../components/Layout/Layout.astro";
import LayoutPage from "../components/Layout/LayoutPage.astro";
import { useAuthenticationStore } from "@/app/store/authentication";
import "../styles/global.css"

---
<script is:inline>
	import { useEffect } from "react";
	import { useAuthenticationStore } from "@/app/store/authentication";

	const token = useAuthenticationStore.getState().token;

	useEffect(() => {
		if (token) {
			window.location.href= "/app"; 
		}
	}, []);

	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>



<script>
	document.addEventListener('DOMContentLoaded', () => {
		const getThemePreference = () => {
			if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
				return localStorage.getItem('theme');
			}
			return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
		};
		const isDark = getThemePreference() === 'dark';
		document.documentElement.classList[isDark ? 'add' : 'remove']('dark');

		if (typeof localStorage !== 'undefined') {
			const observer = new MutationObserver(() => {
				const isDark = document.documentElement.classList.contains('dark');
				localStorage.setItem('theme', isDark ? 'dark' : 'light');
			});
			observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
		}
	});
</script>

<style>
	.hero-section {
		background-image: url('/images/bgStockly.png');
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		background-attachment: fixed;
		min-height: 100vh;
	
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
	}

	/* Overlay para mejorar la legibilidad del texto */
	.hero-section::before {
		content: '';
		position: absolute;
		align-items: center;
		display: flex;
		justify-content: center;

		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		
		z-index: 1;
	}

	.hero-content {
		position: relative;
		z-index: 2;
		text-align: center;
		color: #18429c;
		max-width: 600px;
		width: 95%;
		padding: 2rem;
		
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.hero-logo {
		width: 180px;
		height: 180px;
		
		filter: drop-shadow(0 4px 8px rgba(22, 14, 138, 0.5));
	}

	.hero-title {
		font-size: 3rem;
		font-weight: bold;
		
		text-shadow: 2px 2px 4px rgba(22, 14, 138, 0.5);
	}

	.hero-subtitle {
		font-size: 1.25rem;
		
		opacity: 0.9;
		text-shadow: 1px 1px 2px rgba(1, 7, 82, 0.5);
	}

	.hero-button {
		display: inline-block;
		padding: 0.75rem 2rem;
		background: #5a76aa;
		color: white;
		text-decoration: none;
		border-radius: 0.5rem;
		font-weight: 600;
		transition: all 0.3s ease;
		box-shadow: 0 4px 15px rgba(2, 13, 77, 0.4);
	}

	.hero-button:hover {
		transform: translateY(-2px);
		background: #e4e7e7;
		color: #1a237e;
	}

	/* Responsive design */
	@media (max-width: 768px) {
		.hero-section {
			background-attachment: scroll; /* Mejor rendimiento en móviles */
		}

		.hero-logo {
			width: 150px;
			height: 150px;
		}

		.hero-title {
			font-size: 2.5rem;
		}

		.hero-subtitle {
			font-size: 1.1rem;
		}
	}
</style>

<Layout title="STOCKLY">
	<LayoutPage>
		<section class="hero-section">
			
			<div class="hero-content bg-white/30 backdrop-blur border-b border-white/30 rounded-xl ">
				<img src="/images/gdcsr.png" alt="Logo STOCKLY" class="hero-logo" />
				<h1 class="hero-title">Bienvenido a STOCKLY</h1>
				<p class="hero-subtitle">Manejo de stock simple y eficiente</p>
				<a href="/login" class="hero-button">
					Entrar
				</a>
			</div>
		</section>
	</LayoutPage>
</Layout>
